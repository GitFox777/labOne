#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <locale.h>


int main()
{
    float s,a,b,c,d,e,g,z,x,y;
    
    printf ("Введите переменную x,y,z \n");
    scanf ("%f,%f,%f",&x,&y,&z);
    dfv
    a= pow(9+ (pow(x-y,2)),0.333333);
    b= pow(x,2)+pow(y,2)+2;
    c= a/b;
    e= tan(z)*tan(z)*tan(z);
    d= exp(fabs(x-y));
    g= d*e;
    s= c-g;
    r
    printf ("Ответ: a = %f",a);
 dvt   printf ("Ответ: b = %f",b);
    printf ("Ответ: e = %f",e);
    printf ("Ответ: d = %f",d);
    printf ("Ответ: s = %f",s);

    return 0;
}